# AI Video Agent Plugin 开发合同

## 项目定位

本项目不是视频 App，不做剪辑软件替代品。

目标：开发一个可被多个 AI Agent 调用的视频能力插件。

核心价值：让 Agent 获得理解视频、分析内容、自动剪辑、生成成片的能力。

## 角色分工

产品负责人：定义用户价值、MVP边界、验收标准。

开发 Agent：负责技术实现，必须遵守本文档。

测试 Agent：负责验证真实视频任务闭环。

## 开发原则

1. MVP 优先，不提前开发复杂编辑器。
2. 所有能力必须通过工具接口暴露。
3. 优先解决自动剪视频闭环。
4. 保持未来兼容 MCP 和外部编辑器。
5. 禁止偏离为普通剪辑 SaaS。

## 第一阶段交付

输入：一个长视频文件。

输出：多条可发布短视频。

必须包含：
- 视频分析
- 语音转文字
- 高光识别
- 自动切片
- 字幕生成
- 视频导出

## 验收标准

人工提供一小时采访视频。
系统可以自动生成至少3条60秒左右短视频，并保留剪辑依据。